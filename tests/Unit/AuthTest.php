<?php

use App\Http\Middleware\Auth;
use Tests\TestCase;
use Illuminate\Http\Request;


class AuthTest extends TestCase {
    # TODO: test that the middleware returns 401 if the AUTH_TOKEN is not set or is incorrect.
}
